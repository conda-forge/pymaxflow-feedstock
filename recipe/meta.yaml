{% set name = "PyMaxflow" %}
{% set version = "1.2.15" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/PyMaxflow-{{ version }}.tar.gz
  sha256: 513fb0a2901da803e2a1159c00c3f716f580bc40f7b272fe4638731daca8231a

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - cython
    - numpy
    - pip
    - python
  run:
    - python
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - maxflow
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/pmneila/PyMaxflow
  summary: A mincut/maxflow package for Python
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - martibosch
